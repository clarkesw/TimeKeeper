@echo off

REM Use the 'start' command to run the Python script in a separate, non-waiting window
REM The "" is the title for the new window, which must be the first argument after 'start'
start "" pythonw server.py